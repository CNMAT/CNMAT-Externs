     resource 'vers' (1) {
          0x03,               // Major release: digit before decimal
          0x00,               // Minor release: digits after decimal
          release,               // dev/alpha/beta/release
          0x01,               // Build # (for pre-release)
          0,                  // Region code (US)
          "3.0.1 (OSX)",        // Major/minor release as short string
          "3.0.1, © 2002 CNMAT (OSX Version)"
          };
