     resource 'vers' (1) {
          0x01,               // Major release: digit before decimal
          0x03,               // Minor release: digits after decimal
          beta,               // dev/alpha/beta/release
          0x00,               // Build # (for pre-release)
          0,                  // Region code (US)
          "1.3 (OSX)",        // Major/minor release as short string
          "1.3beta, © 2002 CNMAT (OS X Version)"
          };
