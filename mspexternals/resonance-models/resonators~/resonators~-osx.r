     resource 'vers' (1) {
          0x01,               // Major release: digit before decimal
          0x06,               // Minor release: digits after decimal
          beta,               // dev/alpha/beta/release
          0x00,               // Build # (for pre-release)
          0,                  // Region code (US)
          "1.6 (OSX)",        // Major/minor release as short string
          "1.6beta, © 2002 CNMAT (OS X Version)"
          };
